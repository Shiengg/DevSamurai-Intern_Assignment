# Base URL of the NestJS API (no trailing slash). Required at build time for production.
# Local:
VITE_API_URL=http://localhost:3000
# Deployed API example:
# VITE_API_URL=https://your-api.example.com
